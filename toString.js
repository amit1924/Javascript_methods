let fruits = ["mango", "apple", "grapes", "oranges"];

let result = fruits.toString();

console.log(result);

// join() behaves same like toString but in addition you can specify the separator:

let result1 = fruits.join(" and ");
console.log(result1);
